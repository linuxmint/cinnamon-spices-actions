[Nemo Action]
Name=Convert to PDF
Comment=Convert documents and images to PDF using LibreOffice
Exec=sh -c 'cd "$(dirname "$1")" && libreoffice --headless --convert-to pdf "$1"' -- %F
Selection=s
Icon-Name=application-pdf
Extensions=nodirs;
Dependencies=libreoffice;
